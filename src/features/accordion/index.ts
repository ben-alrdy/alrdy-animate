import type { FeatureModule } from '../../core/registry'

const accordionFeature: FeatureModule = {
  name: 'accordion',
  requiredPlugins: [],
  init: () => () => {},
}

export default accordionFeature
